{
  "name": "soma",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "echo 'Use: cd frontend && npm run dev OR cd backend && npm run dev'",
    "build": "echo 'Use: cd frontend && npm run build OR cd backend && npm run build'"
  }
}
